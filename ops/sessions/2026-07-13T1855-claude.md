# Handoff — 2026-07-13T18:55 — claude (Fable 5, agent-swarm session)
## Task
Execute doc 07 Level 1.5 (pre-delivery hardening): critique the DoD, fix
everything fixable before hardware, prove the gates run — via paired
doer/validator agent workflows.
## System state at exit
mode: pre-delivery (no hardware yet)
branch/commit: master @ def6238   uncommitted: yes (this handoff + STATE/DECISIONS only)
lanes: BRAIN=n/a CODER=n/a FLEET=n/a gateway=n/a (no hardware)
## Done this session (verified by running)
- Gateway routing fix — compose-assembled *_API_BASE URLs; validator traced
  LiteLLM source to prove both the original bug AND that an empty-but-set
  LITELLM_MASTER_KEY 401s everything (hence the commented :? compose line);
  both YAMLs parse, uncomment-paths tested programmatically
- NCCL busbw formula (factor 1 for 2 ranks, was 2×) — re-derived
  independently by validator; per-node HCA derivation; per-var -e flags;
  MTU 9000 in ephemeral fabric path — bash -n green
- eval/selftest.sh GREEN end-to-end on this machine: all four gates pass
  against eval/mock_openai_server.py AND fail correctly under four injected
  fault modes (wrong recall, broken JSON, tools-in-content, 500s)
- ops/manifest.sh --verify PASS (37 files); tamper test demonstrated
  (1-byte change → FAIL + exit 1 → restore → PASS)
- Consistency sweep: ports/container names/gate labels/commands aligned;
  cluster-lane README gate headers renumbered to canonical doc 03 order
  (G4=behavior, G5=needle)
## Written but NOT verified
- ops/systemd/ units — systemd syntax reviewed only; no systemd on this
  Windows box. Verify with systemd-analyze on Spark A before enabling.
- ops/CHECKLIST.md / PINS.md / evidence templates — cross-checked against
  docs 02/03/04 field-by-field, but never exercised against real hardware
  (by definition; they are Level 2 instruments)
- day0/* and lanes/* changes are logic-reviewed + bash -n only — no GB10 to
  execute against
## Discovered values / landmines
- LiteLLM resolves os.environ/VAR ONLY as an entire config value — never
  inline inside a URL. The compose file assembles full URLs; keep it so.
- LiteLLM enables master-key auth from a set-but-EMPTY LITELLM_MASTER_KEY
  env var alone (proxy_server falls back to the env var even with no
  master_key config line) → every route 401s. That is why the compose env
  line ships commented with :? fail-fast expansion.
- DO NOT: "fix" the busbw formula in day0/03 back to 2×bytes/dt — the
  2*(n-1)/n factor is 1 at n=2; the comment in the script says so.
- frontier pin is anthropic/claude-sonnet-5 as of 2026-07-13
  (claude-opus-4-8 = higher-capability alternative); recheck on arrival
  per ops/PINS.md.
## Gates & regression status
- No hardware gates run (impossible pre-delivery). Gate HARNESS itself is
  the thing validated: eval/selftest.sh green; treat it as the regression
  test for any future eval/ change.
- Doc 07: Level 1.5 all boxes checked. Level 2 untouched.
## Dead ends (so nobody repeats them)
- First master-key attempt passed LITELLM_MASTER_KEY=${LITELLM_MASTER_KEY:-}
  unconditionally into the container — bricked every route (see landmine).
  Compose cannot conditionally omit an env entry; commented-line-with-:? is
  the correct shape.
## Next actions (ordered, max 5)
1. On hardware arrival: run ops/PINS.md reverify-on-arrival checklist FIRST
2. Then ops/CHECKLIST.md Day 0 top-to-bottom (stop on any red)
3. Fill config/cluster.env from the example; rsync kit to both nodes
4. Day 1 per CHECKLIST; record evidence with ops/evidence/ templates
5. After ANY future kit edit: bash eval/selftest.sh && ops/manifest.sh
   (regen) — keep both green
## Open questions for VR
- Push this repo to a remote (GitHub private)? Currently local-only.
- LITELLM_MASTER_KEY: enable on Day 1 or accept tailnet+LAN-open gateway?
