# Secrets & machine-local config — NEVER commit
config/cluster.env

# Cluster-lane checkout artifacts (upstream clone lives outside the kit)
PINNED_COMMIT.txt
dsv4-lane/

# OS noise
Thumbs.db
.DS_Store
